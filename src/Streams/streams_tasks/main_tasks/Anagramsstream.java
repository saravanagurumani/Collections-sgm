package Streams.streams_tasks.main_tasks;

import java.util.*;
import java.util.stream.IntStream;

public class Anagramsstream {
    public static void main(String[] args) {
        String str="what is anagram that words like listen silent";
        List<String> list=List.of(str);
//        Arrays.stream().
//        collections.List<Character>list=str.chars().mapToObj(x->(char)x)
//                .toList();
//        System.out.println(list);
//                .forEach(System.out::println);
        System.out.println(list);

    }
}
